generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Afiliado {
  id        String @id @default(uuid())
  nome      String
  email     String @unique
  CriadoEm  DateTime @defualt(now())
}